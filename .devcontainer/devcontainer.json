{
    "name": "un_sdgs",
    "build": {
        "dockerfile": "Dockerfile",
        "context": ".."
    },
    "settings": {
        "terminal.integrated.profiles.linux": {
            "bash (login)": {
                "path": "bash",
                "args": [
                    "-l"
                ]
            }
        }
    },
    "postCreateCommand": "poetry install"
}